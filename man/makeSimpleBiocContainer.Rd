% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeSimpleBiocContainer.R
\name{makeSimpleBiocContainer}
\alias{makeSimpleBiocContainer}
\title{Create a Simple Bioconductor Container}
\usage{
makeSimpleBiocContainer(
  package = NULL,
  container = "mycontainer",
  includeFolder = NULL
)
}
\arguments{
\item{package}{An optional vector of package names. If not provided, the
currently attached packages are used.}

\item{container}{\code{character(1)} with the name of the container. This name
will be used to create the directory for the Docker file and optional
data and scripts.}

\item{data}{Optional \code{character()} with the paths to one or multiple data to
be included in the container in \verb{/home/rstudio/data/}. Missing files
will lead to errors.}

\item{script}{Optional \code{character()} with the paths to one or multiple
scripts to be included in the container in
\verb{/home/rstudio/script/}. Missing files will lead to errors.}
}
\value{
The function returns a \code{character(1)} with the path to the container
directory. It is used for its side effect of creating and populating the
directory.
}
\description{
This function is the main working horse the the package. It
creates a container directory and populates it with a Dockerfile and
optional data and script directories. The container is ready to be
build, pushed and shared with collaborators.
}
\examples{

## Run this in a temporary directory
oldwd <- getwd()
setwd(tempdir())
file.create("data.txt")
makeSimpleBiocContainer(package = "BiocVersion",
                        data = "data.txt")
setwd(oldwd)
}
\author{
SimpleBiocContainer authors
}
